import Layout from '@/Layout/admin';
import React from 'react'

const ServiceInfo = () => {
  return (
    <div>ServiceInfo</div>
  )
}
ServiceInfo.getLayout = (page) => <Layout>{page}</Layout>;
export default ServiceInfo